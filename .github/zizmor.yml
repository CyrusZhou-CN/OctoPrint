rules:
  dangerous-triggers:
    ignore:
      - add_to_backlog.yml
      - pr_automation.yml
  excessive-permissions:
    disable: true
  unpinned-uses:
    config:
      policies:
        OctoPrint/actions/*: any
